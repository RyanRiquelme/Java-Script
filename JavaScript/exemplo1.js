// Estrutura de Repetição!

const array = ['X-tudo', 'X-Egg-Bacon', 'Coxinha',
    'Pizza de Frango', 'Pastel de Frango', 'Quibe', 'Esfirra']; // 7

for (let index = 0; index < array.length; index++) {
    const elemento = array[index];
    console.log(index + 1,elemento)
}